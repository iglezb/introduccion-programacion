"""
Definir una función generar_n_caracteres() que tome un entero n y devuelva el
caracter multiplicado por n. Por ejemplo: generar_n_caracteres(5,def "x") debería devolver
"xxxxx"
"""
# Definimos una funcion que generará una que se repite dicho caracter por el numero de veces que introducimos
def gen(x):
    return letra * numero
# Definimos las variables para la letra a generar
print("Introduce una letra para generar")
letra = input()
# Definimos el numero de veces que queremos generar
print("¿Cuantas veces quieres generarlo?")
numero = int(input())
# Mostramos el resultado
print("El resultado es ",gen(letra))